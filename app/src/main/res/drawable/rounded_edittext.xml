<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/background" /> <!-- Background color -->
    <stroke
        android:width="1dp"
        android:color="@color/dark_gray" /> <!-- Border color and width -->
    <corners android:radius="8dp" /> <!-- Corner radius for rounded corners -->
    <padding
        android:left="12dp"
        android:top="12dp"
        android:right="12dp"
        android:bottom="12dp" />
</shape>
